// NOTE: When you declare a variable with `let`, it is immutable (cannot be changed).
// NOTE: You cannot change the type of a variable even if you declare it as `mut`

fn main() {
    let mut my_number = 8;
    my_number = 10;

    println!("{my_number}");
}
